[algokit]
min_version = "v2.0.0"

[project]
type = "contract"
name = "parity_squad"

[project.run]
build = { commands = ["python -m smart_contracts build"] }
deploy = { commands = ["python -m smart_contracts deploy"] }
