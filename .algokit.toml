[algokit]
min_version = "v2.0.0"

[project]
type = "contract"
name = "parity_squad"

[project.run]
build = { commands = [".venv/bin/python -m smart_contracts build"] }
deploy = { commands = [".venv/bin/python -m smart_contracts deploy"] }
