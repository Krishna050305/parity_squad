[project]
name = "smart_contracts"
version = "0.1.0"
dependencies = [
    "algorand-python>=1.0.0",
    "puyapy>=1.0.0",
]

[tool.poetry]
name = "smart_contracts"
version = "0.1.0"
description = "LendPool smart contracts"
authors = ["Author <author@example.com>"]

[tool.poetry.dependencies]
python = "^3.12"
algorand-python = "^1.0.0"
puyapy = "^1.0.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
